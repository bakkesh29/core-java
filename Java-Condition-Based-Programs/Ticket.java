class Ticket {

    static int getPriceByPlace(String place){
        System.out.println("Executing place: " + place);

        if (place == "Bangalore Urban") {
            System.out.println("Ticket price for Bangalore Urban is 30");
            return 30;
        }

        if (place == "Bangalore Rural") {
            System.out.println("Ticket price for Bangalore Rural is 30");
            return 30;
        }

        if (place == "Mysore") {
            System.out.println("Ticket price for Mysore is 30");
            return 30;
        }

        if (place == "Mandya") {
            System.out.println("Ticket price for Mandya is 30");
            return 30;
        }

        if (place == "Tumkur") {
            System.out.println("Ticket price for Tumkur is 30");
            return 30;
        }

        if (place == "Kolar") {
            System.out.println("Ticket price for Kolar is 30");
            return 30;
        }

        if (place == "Chikkaballapur") {
            System.out.println("Ticket price for Chikkaballapur is 30");
            return 30;
        }

        if (place == "Ramanagara") {
            System.out.println("Ticket price for Ramanagara is 30");
            return 30;
        }

        if (place == "Hassan") {
            System.out.println("Ticket price for Hassan is 30");
            return 30;
        }

        if (place == "Chikkamagaluru") {
            System.out.println("Ticket price for Chikkamagaluru is 30");
            return 30;
        }

        if (place == "Dakshina Kannada") {
            System.out.println("Ticket price for Dakshina Kannada is 30");
            return 30;
        }

        if (place == "Udupi") {
            System.out.println("Ticket price for Udupi is 30");
            return 30;
        }

        if (place == "Kodagu") {
            System.out.println("Ticket price for Kodagu is 30");
            return 30;
        }

        if (place == "Shivamogga") {
            System.out.println("Ticket price for Shivamogga is 30");
            return 30;
        }

        if (place == "Davangere") {
            System.out.println("Ticket price for Davangere is 30");
            return 30;
        }

        if (place == "Chitradurga") {
            System.out.println("Ticket price for Chitradurga is 30");
            return 30;
        }

        if (place == "Ballari") {
            System.out.println("Ticket price for Ballari is 30");
            return 30;
        }

        if (place == "Vijayanagara") {
            System.out.println("Ticket price for Vijayanagara is 30");
            return 30;
        }

        if (place == "Belagavi") {
            System.out.println("Ticket price for Belagavi is 30");
            return 30;
        }

        if (place == "Bagalkot") {
            System.out.println("Ticket price for Bagalkot is 30");
            return 30;
        }

        if (place == "Vijayapura") {
            System.out.println("Ticket price for Vijayapura is 30");
            return 30;
        }

        if (place == "Gadag") {
            System.out.println("Ticket price for Gadag is 30");
            return 30;
        }

        if (place == "Dharwad") {
            System.out.println("Ticket price for Dharwad is 30");
            return 30;
        }

        if (place == "Haveri") {
            System.out.println("Ticket price for Haveri is 30");
            return 30;
        }

        if (place == "Uttara Kannada") {
            System.out.println("Ticket price for Uttara Kannada is 30");
            return 30;
        }

        if (place == "Kalaburagi") {
            System.out.println("Ticket price for Kalaburagi is 30");
            return 30;
        }

        if (place == "Yadgir") {
            System.out.println("Ticket price for Yadgir is 30");
            return 30;
        }

        if (place == "Raichur") {
            System.out.println("Ticket price for Raichur is 30");
            return 30;
        }

        if (place == "Koppal") {
            System.out.println("Ticket price for Koppal is 30");
            return 30;
        }

        if (place == "Bidar") {
            System.out.println("Ticket price for Bidar is 30");
            return 30;
        }

        else {
            System.out.println("Invalid place: " + place);
        }

        return 0;
    }
}