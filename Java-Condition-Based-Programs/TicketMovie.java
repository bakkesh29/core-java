class TicketMovie {

    static int getPriceByMovie(String movie, String seatType, String theater){
        System.out.println("Executing: " + movie + " " + seatType + " " + theater);

        if (movie == "Movie1" && seatType == "Gold" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie2" && seatType == "Silver" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie3" && seatType == "Gold" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie4" && seatType == "Silver" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie5" && seatType == "Gold" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie6" && seatType == "Silver" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie7" && seatType == "Gold" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie8" && seatType == "Silver" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie9" && seatType == "Gold" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie10" && seatType == "Silver" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie11" && seatType == "Gold" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie12" && seatType == "Silver" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie13" && seatType == "Gold" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie14" && seatType == "Silver" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie15" && seatType == "Gold" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie16" && seatType == "Silver" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie17" && seatType == "Gold" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie18" && seatType == "Silver" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie19" && seatType == "Gold" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie20" && seatType == "Silver" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie21" && seatType == "Gold" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie22" && seatType == "Silver" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie23" && seatType == "Gold" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie24" && seatType == "Silver" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie25" && seatType == "Gold" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie26" && seatType == "Silver" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie27" && seatType == "Gold" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie28" && seatType == "Silver" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie29" && seatType == "Gold" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie30" && seatType == "Silver" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie31" && seatType == "Gold" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie32" && seatType == "Silver" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie33" && seatType == "Gold" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie34" && seatType == "Silver" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie35" && seatType == "Gold" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie36" && seatType == "Silver" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie37" && seatType == "Gold" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie38" && seatType == "Silver" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie39" && seatType == "Gold" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie40" && seatType == "Silver" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie41" && seatType == "Gold" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie42" && seatType == "Silver" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie43" && seatType == "Gold" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie44" && seatType == "Silver" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie45" && seatType == "Gold" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie46" && seatType == "Silver" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie47" && seatType == "Gold" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie48" && seatType == "Silver" && theater == "Cinepolis") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie49" && seatType == "Gold" && theater == "PVR") {
            System.out.println("Ticket price is 50"); return 50;
        }

        if (movie == "Movie50" && seatType == "Silver" && theater == "INOX") {
            System.out.println("Ticket price is 50"); return 50;
        }

        else {
            System.out.println("Invalid combination");
        }

        return 0;
    }
}